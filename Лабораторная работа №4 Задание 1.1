#include <iostream>
using namespace std;

double sum(int n) {
	double s = 0;
	int i;
	for (i = 1; i <= n; i++) {
		s += 1.0 / (i * i + 10);
	}
	return s;
}

int main() {
	int n;
	cout << "n = ";
	cin >> n;
	cout << "\n" << "sum = " << sum(n);
	return 0;
}
